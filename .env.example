JWT_SECRET=replace-with-a-long-random-secret
APP_BASE_URL=http://localhost:3000
# Use an Atlas cluster or a MongoDB replica set. Entity writes use transactions to
# atomically advance users.syncVersion and stamp the changed document.
MONGODB_URI=mongodb://localhost:27017/?replicaSet=rs0
MONGODB_DB_NAME=messenger

# ---------------------------------------------------------------------------
# Blob storage (user/agent/market avatars)
# BLOB_BACKEND:
#   auto (default) - use the Vercel Blob backend when BLOB_READ_WRITE_TOKEN is
#                    set, otherwise the self-hosted filesystem backend
#   fs             - filesystem backend (BLOB_STORAGE_DIR), ideal for
#                    self-hosting and local development, no Vercel account needed
#   vercel         - force the Vercel Blob backend (vercel-blob-nonvercel SDK)
# ---------------------------------------------------------------------------
BLOB_BACKEND=auto
BLOB_STORAGE_DIR=./.blobs

# The variables below are only needed by the Vercel Blob backend
# (BLOB_BACKEND=vercel / a Vercel deployment). Leave them unset for
# local development and self-hosting.
# BLOB_READ_WRITE_TOKEN=vercel_blob_rw_local_emulator_secret
# BLOB_STORE_ID=local
# VERCEL_BLOB_API_URL=https://api.vercel.com/blob
# VERCEL_BLOB_STORAGE_URL=
# VERCEL_BLOB_RETRIES=0
